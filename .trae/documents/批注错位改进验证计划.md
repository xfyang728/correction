# 批注错位改进验证计划

## 摘要

本计划承接上一会话的工作。三项改进（坐标空间统一 / 多 OCR 记录匹配 / 合并题拆分 rescale）的代码变更和单元测试已全部完成，但**尚未运行任何测试验证**。本计划仅描述验证步骤，不涉及新的代码改动。

---

## 一、当前状态（基于代码探索确认）

### 1.1 已完成的代码变更

| 改进 | 文件 | 关键变更 | 状态 |
|------|------|---------|------|
| **改进1: page_level 路径坐标空间统一** | [image_processor.py](file:///d:\MyCode\correction\auto_marker\core\image_processor.py) | 新增 `preprocess_image_with_angle` (L36) + `apply_deskew` (L155)；`_deskew_fast` 改返回 `(ndarray, float)` (L92)；`preprocess_image`/`deskew_only` 向后兼容 | ✅ 已完成 |
| **改进1: page_level 分支应用相同角度** | [pipeline_service.py](file:///d:\MyCode\correction\auto_marker\services\pipeline_service.py#L213-L260) | Step A 改用 `preprocess_image_with_angle`；page_level 分支用 `apply_deskew(original_np, deskew_angle)` 替换 `deskew_only` | ✅ 已完成 |
| **改进2: 多 OCR 记录匹配同一手写框** | [handwriting_recognizer.py](file:///d:\MyCode\correction\auto_marker\core\handwriting_recognizer.py#L105-L174) | Step 1 移除 `pop`，用 `matched_box_ids: set[int]` 追踪；新增 Step 1.5 按 x_center 排序拼接，取 min score | ✅ 已完成 |
| **改进3: 合并题拆分路径复用 rescale** | [qwen_vl_recognizer.py](file:///d:\MyCode\correction\auto_marker\core\qwen_vl_recognizer.py#L1184-L1196) | `_split_merged_json_chars` 中 `_clamp_chars_to_column` 传 `handwriting_boxes`（非 None）；插入 `_rescale_chars_x_to_region` 调用，传 `answer_text` | ✅ 已完成 |

### 1.2 已完成的单元测试

| 测试文件 | 测试类数 | 测试用例数 | 关键测试 |
|---------|---------|-----------|---------|
| [test_image_processor.py](file:///d:\MyCode\correction\auto_marker\tests\test_image_processor.py) | 6 类 | 20 例 | `TestApplyDeskew.test_valid_angle_rotates_image`、`TestCoordinateSpaceConsistency.test_same_angle_applied_to_both_images` |
| [test_handwriting_recognizer.py](file:///d:\MyCode\correction\auto_marker\tests\test_handwriting_recognizer.py) | 5 类 | 12 例 | `TestMultipleRecordsMatchSameBox.test_two_records_match_same_box`、`test_records_merged_by_x_order` |
| [test_qwen_vl_recognizer.py](file:///d:\MyCode\correction\auto_marker\tests\test_qwen_vl_recognizer.py) | 扩展 1 类 | +5 例 | `TestSplitMergedJsonChars.test_rescale_applied_to_split_chunks`、`test_handwriting_boxes_not_none` |

### 1.3 待验证的假设

- **假设**：三项改进不会破坏现有功能，且能改善坐标精度
- **验证方式**：单元测试全通过 + E2E 测试通过 + 视觉对比批注 PDF

---

## 二、验证步骤

### 步骤 1: 运行单元测试（无外部依赖）

**目的**：验证三项改进的代码逻辑正确，且不破坏既有测试。

**命令**：
```bash
cd d:\MyCode\correction\auto_marker && python -m pytest tests/test_image_processor.py tests/test_handwriting_recognizer.py tests/test_qwen_vl_recognizer.py -v
```

**预期结果**：
- `test_image_processor.py`: 20 例通过
- `test_handwriting_recognizer.py`: 12 例通过
- `test_qwen_vl_recognizer.py`: 既有 40 例 + 新增 5 例 = 45 例通过
- 总计 77 例通过，0 例失败

**重点关注**：
- `TestMultipleRecordsMatchSameBox.test_two_records_match_same_box` — 改进2核心
- `TestSplitMergedJsonChars.test_rescale_applied_to_split_chunks` — 改进3核心
- `TestApplyDeskew.test_valid_angle_rotates_image` — 改进1核心
- `TestCoordinateSpaceConsistency.test_same_angle_applied_to_both_images` — 改进1场景验证

**失败处理**：
- 若 `test_image_processor.py` 失败：检查 `preprocess_image_with_angle` 返回值类型和 `apply_deskew` 阈值
- 若 `test_handwriting_recognizer.py` 失败：检查 `matched_box_ids` 追踪逻辑和 x 排序拼接
- 若 `test_qwen_vl_recognizer.py` 失败：检查 `_split_merged_json_chars` 中 rescale 调用参数和 `_clamp_chars_to_column` 签名

### 步骤 2: 运行其他相关单元测试（回归验证）

**目的**：验证三项改进未破坏既有功能。

**命令**：
```bash
cd d:\MyCode\correction\auto_marker && python -m pytest tests/test_grader.py tests/test_layout_analyzer.py -v
```

**预期结果**：所有既有测试通过。

### 步骤 3: 清除 OCR 缓存

**目的**：避免旧缓存导致坐标偏移（项目规则要求：代码变更影响坐标定位时必须清缓存）。

**操作**：删除 `d:\MyCode\correction\auto_marker\data\cache\d680a9aa1db832dd74f7eef84b46486b_p0.pkl`

**命令**：
```bash
del "d:\MyCode\correction\auto_marker\data\cache\d680a9aa1db832dd74f7eef84b46486b_p0.pkl"
```

### 步骤 4: 运行 E2E 测试

**前置条件**：
- Qwen3-VL llama.cpp server 运行在 `localhost:8080`
- PaddleOCR 模型已下载
- 测试 PDF 存在：`data/test_samples/301_2026-06-18_003.pdf`

**命令**：
```bash
cd d:\MyCode\correction\auto_marker && python -m pytest tests/test_e2e_annotation.py -v -s
```

**预期结果**：
- 13 例 E2E 测试全部通过
- 匹配 10 道题（含 ①②下联）
- 字符数 ≥ 50（历史 57）
- Y 坐标准确率 100%（57/57）
- X 坐标平均误差 < 100px（改进前 61px）

**重点关注**：
- `test_x_coordinates_preserved` — 验证改进1/3 的 X 坐标修正
- `test_y_coordinates_in_range` — 验证 Y clamp 未被破坏
- `test_question_count` — 验证 10 题匹配未被破坏

### 步骤 5: 视觉验证（可选）

**目的**：人工对比批注 PDF 与原手写位置。

**操作**：
1. 查看 `data/output/301_2026-06-18_003_annotated.pdf`
2. 重点关注：
   - 合并题（如 (7)(8)）拆分后是否各题位置正确
   - 多 OCR 记录拼接的字是否连续无遗漏
   - 整体批注是否对齐手写原位

---

## 三、假设与决策

### 3.1 假设
- 三项改进的代码变更已完整提交（基于代码探索确认）
- 单元测试覆盖了改进的核心逻辑（基于测试文件审查确认）
- E2E 测试前置条件可满足（Qwen3-VL 服务可启动）

### 3.2 决策
- **不修改既有测试断言**：若 E2E 测试因改进导致预期值变化（如字符数增加），再调整断言
- **不引入新测试**：本计划仅验证已有测试，不新增测试用例
- **失败即回滚**：若单元测试失败且无法快速修复，回滚对应改进的代码变更

### 3.3 验证完成标准
- 单元测试 77 例全部通过
- E2E 测试 13 例全部通过
- 视觉检查无明显错位（可选）

---

## 四、不改进项（已确认无需改动）

基于上一会话的五假设验证结论，以下问题不纳入本次改进：
- 假设 4（PDF 映射公式）：已正确实现，Y 翻转、基线偏移、字号自适应均无问题
- 假设 5（坐标修正过度）：Y-clamp 已改混合策略，X-rescale 已双重触发，无需调整
- 版面分析的高度阈值：实际为 90/55（非用户描述的 70/50），且含题号反向纠正
- 列检测：已实现 `_split_into_columns`
- 题号多层回退：已实现圈号回退和文本关键词回退

---

## 五、执行顺序

1. **运行单元测试**（步骤 1）— 快速验证代码逻辑
2. **运行回归测试**（步骤 2）— 确保未破坏既有功能
3. **清除 OCR 缓存**（步骤 3）— 为 E2E 准备
4. **运行 E2E 测试**（步骤 4）— 端到端验证
5. **视觉验证**（步骤 5，可选）— 人工确认

每步失败则停止并诊断，不继续后续步骤。
