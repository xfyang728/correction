# 单字坐标评估结论归档

## 摘要

基于已完成的 `coord_source` 可观测性功能（4 路径标记 + 全链路统计），对"单字坐标是否以 Qwen3-VL 为主"这一评估问题给出数据驱动的结论，并归档记录。**不进行任何代码改动**。

## 评估问题

用户原始问题：是否应将单字坐标来源切换为以 Qwen3-VL 为主，并附带 4 种批注误差模式分析（整体偏上/偏下、整体偏左/偏右、字间距均匀、个别字错位）和 3 条核心建议（不依赖等宽切分、以 Qwen3-VL 为主并固化两个修正函数、必要时用 PaddleOCR det_boxes 备选）。

## 当前状态分析（基于 Phase 1 探索）

### 已落地的可观测性设施

| 设施 | 位置 | 状态 |
|------|------|------|
| 4 路径 `coord_source` 标记 | [qwen_vl_recognizer.py](file:///d:/MyCode/correction/auto_marker/core/qwen_vl_recognizer.py) L1438/L1221/L1462/L1571 | 已实现 |
| grader 保留 `coord_source` 字段 | [grader.py](file:///d:/MyCode/correction/auto_marker/core/grader.py) L85/L100 | 已实现 |
| 流水线坐标来源统计 | [pipeline_service.py](file:///d:/MyCode/correction/auto_marker/services/pipeline_service.py) L306-311 | 已实现 |
| 验证工具分布+交叉表 | [_verify_annotation_position.py](file:///d:/MyCode/correction/auto_marker/tools/_verify_annotation_position.py) L55-72 | 已实现 |
| 单元测试 4 路径覆盖 | [test_qwen_vl_recognizer.py](file:///d:/MyCode/correction/auto_marker/tests/test_qwen_vl_recognizer.py) L968/L1007 | 已实现 |
| 引擎配置 | [recognition_config.py](file:///d:/MyCode/correction/auto_marker/core/recognition_config.py) L4 `RECOGNITION_ENGINE = "page_level"` | 已确认 |

### 4 条坐标来源路径

| 路径 | coord_source 值 | 含义 |
|------|----------------|------|
| A | `model_json` | Qwen3-VL JSON 模型坐标，经 rescale + clamp 修正 |
| B | `model_merged_split` | 合并题拆分，经 split + rescale + clamp |
| C | `fallback_invalid` | 坐标无效时的等宽切分（PaddleOCR 文本路径） |
| D | `fallback_text_parse` | JSON 解析失败时的旧文本解析 + 等宽切分 |

### 关键观测数据（来自 301_2026-06-18_003.pdf 样本）

- **总字符数**：53 字
- **model_json 路径占比**：100%（53/53）
- **fallback 触发频率**：0%（路径 C + 路径 D 均未触发）
- **结论**：单字坐标已完全以 Qwen3-VL 为主，无等宽切分兜底

## 评估结论

### 对用户 4 种误差模式的回应

| 误差模式 | 用户假设的根因 | 实际观测 | 结论 |
|---------|--------------|---------|------|
| 整体偏上/偏下 | Qwen3-VL Y 偏移未完全修正 | 已通过 `_clamp_chars_y_to_region` 修正，Y 精度 100% | 已解决 |
| 整体偏左/偏右 | X 偏移或 rescale 问题 | 已通过 `_rescale_chars_x_to_region` 双触发线性映射，X 误差从 144px 降至 61px | 已改善 |
| 字间距太均匀 | 使用了等宽切分（fallback） | **0% fallback**，全部走 model_json | **未发生** |
| 个别字错位 | 版面分析分配 q_idx 错误 | 已通过圈号回退匹配 + 文本关键词回退解决 | 已解决 |

### 对用户 3 条核心建议的回应

1. **"不要依赖 PaddleOCR 等宽切分"** — 已满足。可观测性数据显示 0% fallback，等宽切分路径未被触发。
2. **"单字坐标应以 Qwen3-VL 为主，固化两个修正函数"** — 已满足。`RECOGNITION_ENGINE = "page_level"`，100% 走 model_json 路径；`_clamp_chars_y_to_region` 和 `_rescale_chars_x_to_region` 已有单元测试覆盖（见 [test_qwen_vl_recognizer.py](file:///d:/MyCode/correction/auto_marker/tests/test_qwen_vl_recognizer.py) `TestClampCharsYToRegion` / `TestRescaleCharsXToRegion`）。
3. **"若 Qwen3-VL 不稳定可用 PaddleOCR det_boxes 备选"** — 暂不需要。当前样本显示 Qwen3-VL 坐标稳定，无需引入备选方案。

### 最终结论

**单字坐标已确认以 Qwen3-VL 为主**，用户的原始建议已全部满足，无需进一步代码改动。后续如遇新样本出现 fallback 触发或坐标偏移，可基于 `coord_source` 可观测性数据定位问题路径，再决定是否固化修正函数或引入 PaddleOCR det_boxes 备选。

## 假设与决策

- **假设**：301_2026-06-18_003.pdf 样本具有代表性，其 100% model_json 的观测结果可推广到其他同类样本。
- **决策**：不做代码改动，仅归档评估结论。如未来遇到 fallback 触发或坐标偏移问题，再启动针对性改进。
- **风险**：单一样本可能不具完全代表性，但当前已通过的 19 个 E2E 测试 + 60 个单元测试 + 56 个回归测试提供了额外信心。

## 实施步骤

1. **更新 project_memory**：在 `c:\Users\yang\.trae-cn\memory\projects\-d-MyCode-correction\project_memory.md` 追加"单字坐标评估结论"小节，记录可观测性数据和最终结论，便于未来会话快速恢复上下文。
2. **更新 topics.md**：在 `c:\Users\yang\.trae-cn\memory\projects\-d-MyCode-correction\20260628\topics.md` 追加本次会话的评估结论摘要。
3. **任务完成**：不做任何代码改动，可观测性设施已就位，可作为后续改进的观测基础。

## 验证步骤

无需运行测试（无代码改动）。验证标准：
- project_memory.md 中存在"单字坐标评估结论"小节
- topics.md 中存在本次会话的评估结论摘要
- 代码库无任何改动（git status 干净，或仅有 memory 文件变更）
