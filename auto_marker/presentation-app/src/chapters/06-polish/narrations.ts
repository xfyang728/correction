import type { Narration } from "../../registry/types";

export const narrations: Narration[] = [
  "然后是文字偏下的问题。渲染文字时字号有个 24pt 的上限，大框会被截断。但原来的基线公式固定取框底加 20% 高度，框一大文字就挤在下半部分。我把基线公式改成了垂直居中。文字视觉中心严格等于框中心。",
  "最后还有一招。Qwen3-VL 原本能输出每个字的识别置信度，但以前我用的是规则估算的假置信度，门控形同虚设。改用模型真实置信度之后，7 个原本被漏掉的存疑字被正确标橙了。",
  "门控阈值：green ≥ 0.85 / orange ≥ 0.60 / skip < 0.30。7 个存疑字被救回。",
];
